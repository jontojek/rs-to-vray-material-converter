---
source: https://documentation.chaos.com/space/VMAYA/111740567/VRayFlakes2Mtl
site: Chaos V-Ray for Maya Documentation
section: Materials > Common > VRayFlakes2Mtl
captured: 2026-04-09
tags: [vray, maya, material, shader, flakes, car-paint, metallic, raw]
---

# VRayFlakes2Mtl

VRayFlakes2Mtl simulates the metallic flakes found in car paints. It is a complex material generated by mixing several textures. It can be used standalone or inside a VRayBlendMtl to add metallic flakes to other shaders. Uses GGX distribution — fast and memory efficient.

Note: This material is a flakes-only shader. It is commonly layered over a base coat material using VRayBlendMtl to build a complete car paint look (versus VRayCarPaint2Mtl which is a self-contained 3-layer material).

---

## Flake Parameters

**Flake Color** – Color or map of the metal flakes.

**Flake Random Color** – Sets colors from a specified map (e.g. File or Ramp) to flakes in a random pattern. Only the bottom row on the U-axis of the map is sampled for colors. When used, Flake Color acts as a filter — set to white (255,255,255) to disable its effect.

**Flake Orientation** – Orientation of the metal flakes relative to the surface normal. 0.0 = all flakes perfectly aligned with surface. 1.0 = flakes randomly oriented. Values above 0.5 not recommended — can produce artifacts.

**Flake Orientation Tail** – Controls the transition from highlighted to non-highlighted areas in flake reflections.

**Flake Glossiness** – Reflection glossiness of the flakes.

**Flake Density** – Number of flakes per area. Lower = fewer, higher = more. 0.0 = no flakes.

**Flake Scale** – Scales the entire flake structure.

**Flake Scale Triplanar** – Active only when Mapping Type is Triplanar. Scales the entire flake structure.

**Flake Size** – Size of flakes relative to distance between them. Higher = bigger flakes, lower = smaller flakes.

**Flake Map Size** – Internal bitmap resolution for stored flakes. Lower = less RAM but visible tiling. Higher = more RAM, less tiling.

**Flake Seed** – Random seed for flake pattern. Changing produces a different pattern.

**Flake UV Coords** – Connect a texture placement node.

**Mapping Type** – How flakes are mapped to the surface.
- Mapping Channel – Uses the specified UV channel.
- Triplanar – Automatically computes coordinates in object space from surface normals.

**Mapping Channel** – Which UV channel to use when Mapping Type is Mapping Channel.

---

## Options

Note: These options are inactive with V-Ray GPU engine.

**Trace Reflections** – Global switch to toggle reflections for the flakes.

**Trace Depth** – Number of times a ray can be reflected.

**Double Sided** – When enabled, the material is double-sided.

**Cutoff Threshold** – Cutoff threshold for layer reflections.

---

## Environment Override

**Use Environment Override** – When enabled, specifies a local environment map for reflections and refractions.

**Environment Override Texture** – Map for the environment override.

**Environment Override Priority** – Priority for the environment override texture relative to other materials.
